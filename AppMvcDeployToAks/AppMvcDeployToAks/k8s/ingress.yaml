apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: mvc-app

spec:
  ingressClassName: nginx

  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix

        backend:
          service:
            name: mvc-app-service
            port:
              number: 80